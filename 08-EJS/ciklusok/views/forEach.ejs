<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS ciklusok</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Asszociatív tömb elemei 'for' ciklussal</h1>
    <ul>
        <% for (let key in users) { %>
            <li><%= key %>: <%= users[key].name %>(<%= users[key].age %>), <%= users[key].city %></li>
        <% } %>
    </ul>
</body>
</html>